
      var shape = this.add.graphics();
      shape.fillStyle(0xffffff);
      shape.beginPath();
      shape.moveTo(-240, 0);
      shape.arc(-240, 0, 250, 0, Math.PI * 2);
      shape.moveTo(240, 0);
      shape.arc(240, 0, 250, 0, Math.PI * 2);
      shape.fillPath();
      shape.scale = .5
  
